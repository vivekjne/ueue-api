"use strict";
const Menu = use("App/Models/Menu");

class MenuController {
  async getMenuByRestaurant({ params, request, response }) {
    const restaurant_id = params.restaurant_id;
    try {
    } catch (err) {}
  }
}

module.exports = MenuController;
